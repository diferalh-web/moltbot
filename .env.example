# Variables de entorno para Docker Compose (Moltbot)
# Copia a .env y ajusta los valores según tu entorno

# === Acceso vía Tailscale / red remota ===
# URL pública donde se accede a Open WebUI (OAuth, redirects, etc.)
# Reemplaza con tu IP Tailscale (100.x.x.x) o hostname
# WEBUI_URL=http://100.64.1.2:8082
# WEBUI_URL=http://moltbot-pc:8082

# Por defecto (localhost):
# WEBUI_URL=http://localhost:8082

# === APIs opcionales ===
# TAVILY_API_KEY=        # Búsqueda web (opcional)
# GEMINI_API_KEY=       # Gateway externo (opcional)
# HUGGINGFACE_API_KEY=  # Gateway externo (opcional)

# === ComfyUI (opcional) ===
# COMFYUI_CHECKPOINT_NAME=flux1-schnell-fp8.safetensors
