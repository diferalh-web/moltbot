# 📋 Ver Contenido de Archivos de Configuración

## 🔍 Archivos Encontrados

Tienes 3 archivos importantes:
- `auth-profiles.json` - Perfiles de autenticación
- `config.json` - Configuración del agente
- `models.json` - Información de modelos

## ✅ Ver Contenido

**Ejecuta estos comandos en la VM:**

```bash
# Ver config.json
cat ~/.openclaw/agents/main/agent/config.json

# Ver models.json
cat ~/.openclaw/agents/main/agent/models.json

# Ver auth-profiles.json (ya lo vimos antes)
cat ~/.openclaw/agents/main/agent/auth-profiles.json
```

## 🔧 Lo Que Necesitamos Verificar

1. **config.json** debería especificar Ollama como proveedor
2. **models.json** puede tener información sobre qué modelo está usando
3. **auth-profiles.json** ya lo vimos, pero necesitamos asegurar que Ollama esté configurado

## 📝 Después de Ver los Archivos

Comparte el contenido de:
- `config.json`
- `models.json`

Con esa información podremos corregir la configuración para que use Ollama directamente.

---

**Ejecuta los comandos `cat` para ver el contenido de los archivos y comparte los resultados.**












